# Docs Structure

Project documentation is split into two areas:

- `docs/public/`: safe to publish in GitHub
- `docs/private/`: local-only notes, excluded from Git

Before pushing, verify that public docs contain placeholders only and no personal workspace links or secret values.

